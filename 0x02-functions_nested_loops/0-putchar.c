#include <stdio.h>

/**
 * main - the main function
 * putchar - prints out the output of the variable
 * Return: always 0 (success)
 */
int main(void)
{
char p = '_putchar';
putchar(p);
putchar('\n');

return (0);
}
